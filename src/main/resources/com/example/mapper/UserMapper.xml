<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.UserMapper">
    <resultMap id="userResult" type="User">
        <id property="id" column="id" />
        <result property="username" column="username" />
        <result property="password" column="password" />
        <collection property="posts" ofType="Post" column="id" select="getPosts" javaType="ArrayList">
            <id property="id" column="id" />
            <result property="body" column="body" />
            <result property="createDate" column="createDate" />
        </collection>
    </resultMap>
    <select id="getUserById" resultMap="userResult">
        select * from user where id = #{id}
    </select>

    <select id="getPosts" resultType="Post">
        select post.id, post.body, post.createDate from post where post.user_id = #{id}
    </select>

    <select id="countByUsername" resultType="int">
        select count(1) from user where username = #{username}
    </select>
    
    <resultMap id="user" type="User">
        <id property="id" column="id" />
        <result property="username" column="username" />
    </resultMap>
    <select id="getUserByUsernameAndPassword" resultMap="user">
        select id, username from user where username = #{username} and password = #{password}
    </select>
    
    <insert id="addUser" useGeneratedKeys="true" keyProperty="id">
        insert into user (username, password) values(#{username}, #{password})
    </insert>
    
    <update id="updateUser">
        update user set username = #{username}, password = #{password} where id = #{id}
    </update>

    <delete id="deleteUser">
        delete from user where id = #{id}
    </delete>
</mapper>